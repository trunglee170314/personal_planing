-- Run only after 0021_workspace_access.sql and after verifying the exact
-- production project. This email was explicitly confirmed by the app owner.
select public.bootstrap_myplan_admin('lezantrung@gmail.com');
